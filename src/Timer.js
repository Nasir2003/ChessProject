import { useEffect, useState } from "react";

export default function Timer(props) {

  // TODO: Use setInterval to update some "timer"
  // state variable every second.
  
  // let [i , seti] = useState(0);
  // function myFunc(){
  //   seti(i + 1);
  //   console.log(i)
  // }
  // useEffect(() => {
  //   setInterval(myFunc, 60000);
  // });
  return (
    <div>
      {/* <h1>Minutes Elapsed: {i}</h1> */}
    </div>
  );
}